To start project enter in terminal: mvn clean install -DskipTests && docker-compose up -d --build

For this task I spent about 24 hours. The one more thing is that I was never working with MongoDb, Redis and JUnit.
Hope you will rate my work:)